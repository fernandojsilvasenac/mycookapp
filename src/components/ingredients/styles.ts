import { StyleSheet } from "react-native";
import { theme } from '@/theme';

export const styles = StyleSheet.create({
    container:{
        // flexWrap: "wrap",
        // flexDirection: "row",
        paddingBottom:2,
        gap:2,
        height:45
        
    },
})